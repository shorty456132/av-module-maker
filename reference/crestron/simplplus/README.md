# SIMPL+ Reference (WIP)

Drop SIMPL+ help docs / examples here (`.usp`/`.ush` language, C-like syntax,
INPUT/OUTPUT signal model, compiled inside SIMPL Windows).

Follow the Q-SYS convention: each doc keeps a `> Source: <url>` header line.
