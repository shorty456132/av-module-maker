# SIMPL# Patterns (WIP)

> Status: **stub** — to be populated as the SIMPL# skills are built.
> Scope: **SIMPL# only** (`.clz`). Mirrors the role of
> `reference/qsys/QSYS_PATTERNS.md`: ready-to-adapt code patterns so most work
> needs no doc search. Read alongside `SIMPLSHARP_CONSTRAINTS.md`.
>
> Sibling targets: SIMPL+ → `../simplplus/SIMPLPLUS_PATTERNS.md`,
> SIMPL# Pro → `../simplsharp-pro/SIMPLSHARP_PRO_PATTERNS.md`.

## Planned pattern coverage
- SIMPL# class exposing `InputSig` / `OutputSig` to SIMPL Windows.
- Event-driven property glue and `ushort` / `string` join mapping.
- Serial/TCP comms and buffer parsing.

_Add confirmed patterns here, each with a `> Source:` link to the SIMPL# help doc
it derives from. Keep this file SIMPL#-only._
