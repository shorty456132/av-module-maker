# SIMPL# Reference (WIP)

Drop SIMPL# help docs / API reference here (C# / .NET class libraries compiled
to `.clz` and loaded by SIMPL Windows as custom modules).

Follow the Q-SYS convention: each doc keeps a `> Source: <url>` header line.
